[pytest]
pythonpath = src
testpaths = tests

markers =
    ephemeral: Local tests (SQLite / TestClient). Safe for CI; data is not kept.
    seed: POSTs to a running server (BASE_URL). Leaves data for manual demos; skipped in CI by default.
