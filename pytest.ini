[pytest]
pythonpath = src tests
testpaths = tests

markers =
    ephemeral: Local tests (SQLite / TestClient). Safe for CI; data is not kept.
    seed: Hits a live server (BASE_URL): seed or clear scripts; manual only; skipped in CI.
